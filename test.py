import numpy as np
'''
from sympy import *
import math

p, q, r, s = 2.0, 1.23, 1.5, 1.0
a = Symbol('a', positive= True)

def f(a, p, q, r, s):
    return (pow(p, a-1) * gamma(s*a + 1)) / (pow(gamma(a), r) * pow(q, s*a + 1))

integrate((pow(p, a-1) * gamma(s*a + 1)) / (pow(gamma(a), r) * pow(q, s*a + 1)))

#i = decimal.Decimal(quad(f, 0, np.inf, args=(p, q, r, s)))
#print(i)
'''

a = [[0,1],[2,3],[4,5]]

print(a[0])


